import { Concert_One, Jua, Playpen_Sans } from "next/font/google";

export const concertone = Concert_One({
    subsets: ["latin"],
    display: "swap",
    weight: "400",
    preload: false,
})

export const jua = Jua({
    subsets: ["latin"],
    display: "swap",
    weight: "400",
    preload: false,
})

export const playpen = Playpen_Sans({
    subsets: ["latin"],
    display: "swap",
    weight: "400",
    preload: false,
  })